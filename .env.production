# Production — baked in at `next build` (NEXT_PUBLIC_*)
# Must override .env: NEXT_PUBLIC_SSR=true disables `output: export` so no out/ folder.
NEXT_PUBLIC_SSR=false
NEXT_PUBLIC_ADMIN_PANEL_URL=https://backend-api-master.bnutritionmart.in
NEXT_PUBLIC_SITE_URL=https://nainistore.in
